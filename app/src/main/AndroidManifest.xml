<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/tastify"
        tools:targetApi="31">
        <activity
            android:name=".more"
            android:exported="false" />
        <activity
            android:name=".others.searchResult"
            android:exported="false" />
        <activity
            android:name=".recipe"
            android:exported="false" />
        <activity
            android:name=".others.shoppingListActivity"
            android:exported="false" />
        <activity
            android:name=".others.myRecipes"
            android:exported="false" />
        <activity
            android:name=".others.bookMarksActivity"
            android:exported="false" />
        <activity
            android:name=".others.editProfileActivity"
            android:exported="false" />
        <activity
            android:name=".login.SignIn"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/splash_screen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>